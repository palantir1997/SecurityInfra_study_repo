sudo apt install -y apache2 php php-fpm mariadb-server

저장소 설치
sudo wget https://repo.zabbix.com/zabbix/7.0/ubuntu/pool/main/z/zabbix-release/zabbix-release_7.0-1+ubuntu24.04_all.deb

sudo dpkg -i zabbix-release_7.0-1+ubuntu24.04_all.deb
sudo apt update -y

zabbix 설치
sudo apt -y install zabbix-server-mysql zabbix-frontend-php zabbix-apache-conf zabbix-sql-scripts zabbix-agent2 php-mysql php-gd php-bcmath php-net-socket


DB 설정
sudo mysql_secure_installation
sudo systemctl start mariadb
mariadb -u root -p
Password:

>CREATE DATABASE my_zabbix_db character set utf8 collate utf8_bin;

>CREATE USER 'zabbix_user'@'localhost' IDENTIFIED BY 'zabbix123!';

>GRANT ALL PRIVILEGES ON my_zabbix_db.* TO 'zabbix_user'@'localhost' WITH GRANT OPTION;

>FLUSH PRIVILEGES;

>exit;

zabbix 설치 및 db 스크립트 실행
 sudo zcat /usr/share/zabbix-sql-scripts/mysql/server.sql.gz | mysql --default-character-set=utf8mb4 -uzabbix_user -p'zabbix123!' my_zabbix_db

Zabbix 환경 설정
# vi /etc/zabbix/zabbix_server.conf

DBName=my_zabbix_db
DBUser=zabbix_user
DBPassword=zabbix123!

자기 자신 모니터링 설정
# vi /etc/zabbix/zabbix_agent2.conf

Server=127.0.0.1
ServerActive=127.0.0.1
Hostname=zabbix server

서비스 시작                                                                                              
sudo systemctl enable zabbix-server zabbix-agent2 apache2
sudo systemctl restart zabbix-server zabbix-agent2 apache2 

- PHP Configuration
# vi /etc/php/8.3/fpm/pool.d/www.conf

# 맨 마지막에 추가

php_value[max_execution_time] = 300
php_value[memory_limit] = 128M
php_value[post_max_size] = 16M
php_value[upload_max_filesize] = 2M
php_value[max_input_time] = 300
php_value[max_input_vars] = 10000
php_value[always_populate_raw_post_data] = -1
php_value[date.timezone] = Asia/Seoul
:wq
!

- 서비스 재시작
systemctl restart apache2 php8.3-fpm

Zabbix 설치
192.168.100.110/zabbix
-> Next -> Next(OK 확인)

--- locate error 시
sudo apt install -y locales
sudo dpkg-reconfigure locales 를 통해 재구성
97번 선택 후 3번 선택 
sudo systemctl restart apache2 php8.3-fpm 후 다시 설치 진행

-> DB 설정 후 이어서 설치 진행 -> 로그인 (Admin / zabbix)

@ 이거했더니 됨
happy@server:~$ sudo vi /etc/zabbix/web/zabbix.conf.php







####

원인은 해쉬 손상과
관리자 어드민 등급이 roleid가 낮았음




